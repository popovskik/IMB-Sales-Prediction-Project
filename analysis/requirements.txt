# Analysis + report environment (Python 3.13).
# scikit-learn / xgboost / joblib are pinned exactly — api/requirements.txt must
# match these so the serialized .joblib models deserialize without version skew.
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
scikit-learn==1.9.0
xgboost==3.3.0
joblib==1.5.3
matplotlib==3.11.0
pmdarima>=2.0.0

# Test + Quarto (python/jupyter engine for report.qmd)
pytest==9.1.1
nbclient==0.11.0
nbformat==5.10.4
jupyter==1.1.1
