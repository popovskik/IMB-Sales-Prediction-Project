"""Restaurant demand analytics — shared pipeline package.

Importable modules: data, eda, features, models (added across U3-U6).
A single global seed makes the whole study reproducible from a clean checkout.
"""

RANDOM_SEED = 42
