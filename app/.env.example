# Base URL of the deployed FastAPI predictor (Railway). Leave unset for local dev
# (defaults to http://localhost:8000). Set this in Vercel's project env vars.
VITE_API_URL=https://your-api.up.railway.app
